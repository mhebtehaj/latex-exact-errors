\documentclass{article}
\usepackage{amsmath}
\input{macros}
\begin{document}

% The command defined in macros.tex is recognized across files.
$\customsymbol + 1$

% This intentional typo should receive a live yellow highlight.
% Hover over it for a suggestion, then change it to \alpha.
$\alhpa + 1$

% After fixing the typo above, type a single $ or { on a new line.
% Pause briefly to see the unclosed-delimiter highlight, then complete the pair.

\end{document}
